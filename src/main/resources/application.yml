server:
  port: 8880
  
spring:
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
  devtools:
    restart:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.MySQL8Dialect
    database: mysql

  datasource:
    url: jdbc:mysql://codingwasabi-mysql.ccszmnhxhmwx.ap-northeast-2.rds.amazonaws.com:3306/bigtong_db?serverTimezone=Asia/Seoul
    username: admin
    password: 1q2w3e4r!
    driver-class-name: com.mysql.cj.jdbc.Driver

    #logging:
    #level:
    #org.hibernate.SQL: debug

